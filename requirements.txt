# Only what this site actually imports. The previous file was a global
# `pip freeze` (tensorflow, jupyter, selenium, scikit-learn, grpcio...) which
# is why installs broke: grpcio has no wheel for new Pythons and tries to
# build from source.

Django==4.1.7

# Server / static
gunicorn
waitress>=2.1.2
whitenoise

# Config + database (Neon Postgres)
django-environ
dj-database-url
psycopg2-binary

# Media storage (Cloudinary)
cloudinary
django-cloudinary-storage
django-storages

Pillow
